import { OwnerLayout } from '@/components/layout/OwnerLayout';

export default function OwnerSectionLayout({ children }: { children: React.ReactNode }) {
  return <OwnerLayout>{children}</OwnerLayout>;
}
